spring:
  application:
    name: ms-catalogos
  datasource:
    url: jdbc:postgresql://postgres:5432/catalogos
    username: postgres
    password: postgres
  jpa:
    hibernate:
      # 'update' es más seguro para Docker que 'validate' si los scripts de migración no están perfectos.
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  security:
    oauth2:
      resourceserver:
        jwt:
          # Configuración correcta de Keycloak
          issuer-uri: http://keycloak:8084/realms/tpi-backend

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus
  metrics:
    tags:
      application: ms-catalogos

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    use-root-path: true
